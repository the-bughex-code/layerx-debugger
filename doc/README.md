# Documentation assets

Place screenshots and the demo GIF referenced by the root `README.md` here:

- `viewer.png` — the searchable log list with the session-health banner
- `detail.png` — the log detail screen with blame analysis and API diff
- `demo.gif` — a short capture of the floating button and viewer in action
